{
  "name": "docsholder-clinical-core",
  "version": "0.1.0",
  "private": true,
  "description": "DocSholder — núcleo clínico: schemas por patologia, relatório determinístico, métricas de instabilidade, GS1, PROMs",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "typecheck": "tsc --noEmit",
    "seed:sql": "ts-node scripts/generate-seed.ts > db/seeds/002_pathology_catalog.sql",
    "lint:no-recommend": "ts-node scripts/check-forbidden-terms.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
